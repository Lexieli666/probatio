# Probatio price table for the Consilium dogfood suite. VERIFY EVERY NUMBER BEFORE YOU USE IT.
#
# Provenance: Consilium-Health's published run 20260830T170133Z (2026-08-30) called exactly one
# model, named below verbatim as the tapes report it. Consilium ships eval/pricing.yaml empty by
# design, so the rates here were taken from the provider's price page, not from Consilium.
#
# Checked on: 2026-09-04, https://developers.openai.com/api/docs/pricing
# (gpt-4o-mini row: Input $0.15, Output $0.60 per 1M tokens)
#
# Against an empty table every cost ceiling in this suite is reported unenforceable rather than
# passed (docs/providers.md), which is the state examples/consilium/results/replay-unpriced.*
# and the baselines under .probatio/baseline/ record. With this table the ceilings are enforced,
# and because budget results are part of a scores baseline (DECISIONS 68) the priced run keeps
# its own baseline directory, .probatio/baseline-priced/ (DECISIONS 83).

gpt-4o-mini-2024-07-18:
  input_per_mtok: 0.15
  output_per_mtok: 0.60
