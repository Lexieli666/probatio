# BLOCKERS.md — sub-tasks abandoned after three attempts, with the three approaches tried

Nothing is blocked. Entries are added only under the three-strike rule in `CLAUDE.md`, and the
corresponding item is marked `[blocked]` in `PROGRESS.md`.
